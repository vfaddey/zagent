# Contracts

Stable file contracts shared by the launcher, runtime, examples, and external integrations.

These schemas are intentionally outside both Python packages. Runtime and launcher should not
depend on each other through shared Python code.

